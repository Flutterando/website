import 'package:flutter_test/flutter_test.dart';

void main() {
  // initModule(HomeModule());
  // // PackagesController packages;
  // //
  // setUp(() {
  //   //     packages = HomeModule.to.get<PackagesController>();
  // });

  group('PackagesController Test', () {
      test("PackagesController Test", () {
        // expect(packages, isInstanceOf<PackagesController>());
      });

    //   test("Set Value", () {
    //     expect(packages.value, equals(0));
    //     packages.increment();
    //     expect(packages.value, equals(1));
    //   });
  });
}
