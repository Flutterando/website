
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Pass test', () {

  });
//  testWidgets('PackagesWidget has message', (tester) async {
//    await tester.pumpWidget(buildTestableWidget(PackagesWidget()));
//    final textFinder = find.text('Packages');
//    expect(textFinder, findsOneWidget);
//  });
}
