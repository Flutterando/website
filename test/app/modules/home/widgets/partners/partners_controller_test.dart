import 'package:flutter_test/flutter_test.dart';

void main() {
  // initModule(HomeModule());
  // // PartnersController partners;
  // //
  // setUp(() {
  //   //     partners = HomeModule.to.get<PartnersController>();
  // });

  group('PartnersController Test', () {
      test("PartnersController Test", () {
        // expect(partners, isInstanceOf<PartnersController>());
      });

    //   test("Set Value", () {
    //     expect(partners.value, equals(0));
    //     partners.increment();
    //     expect(partners.value, equals(1));
    //   });
  });
}
