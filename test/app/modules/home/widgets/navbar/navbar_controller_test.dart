import 'package:flutter_test/flutter_test.dart';

void main() {
  // initModule(HomeModule());
  // // NavbarController navbar;
  // //
  // setUp(() {
  //   //     navbar = HomeModule.to.get<NavbarController>();
  // });

  group('NavbarController Test', () {
      test("NavbarController Test", () {
        // expect(navbar, isInstanceOf<NavbarController>());
      });

    //   test("Set Value", () {
    //     expect(navbar.value, equals(0));
    //     navbar.increment();
    //     expect(navbar.value, equals(1));
    //   });
  });
}
