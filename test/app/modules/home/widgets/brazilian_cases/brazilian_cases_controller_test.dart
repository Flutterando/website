import 'package:flutter_test/flutter_test.dart';

void main() {
  // initModule(HomeModule());
  // // BrazilianCasesController braziliancases;
  // //
  // setUp(() {
  //   //     braziliancases = HomeModule.to.get<BrazilianCasesController>();
  // });

  group('BrazilianCasesController Test', () {
      test("BrazilianCasesController Test", () {
        // expect(braziliancases, isInstanceOf<BrazilianCasesController>());
      });

    //   test("Set Value", () {
    //     expect(braziliancases.value, equals(0));
    //     braziliancases.increment();
    //     expect(braziliancases.value, equals(1));
    //   });
  });
}
